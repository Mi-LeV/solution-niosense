#include <stdlib.h>
#ifndef __cplusplus
#include <stdbool.h>
#endif

#define FEU_OFFSET_RAIL (10) // offset of feu from rail 
#define FEU_POSTION (1000) // position of feu on rail axis (supposed at the middle point of both distances)
#define DIST_TO_MM (999.9f) // step distance to mm
#define MM_TO_DIST (1.1f) // mm to step distance